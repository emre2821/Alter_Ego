# ───────────────────────────────────────────────────────────────────────────────
# Alter/Ego Environment Configuration
# ───────────────────────────────────────────────────────────────────────────────
# Copy this file to `.env` and customize values for your environment.
# These variables override defaults in alter_ego_config.yaml.
# ───────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────
# Model / LLM Settings
# ─────────────────────────────────────────────────────────────────

# Path to your GPT4All models directory
# GPT4ALL_MODEL_DIR=/path/to/models

# Specific model file to load (must be in models directory)
# GPT4ALL_MODEL=DeepSeek-R1-Distill-Qwen-1.5B-Q4_0.gguf

# ─────────────────────────────────────────────────────────────────
# Persona Settings
# ─────────────────────────────────────────────────────────────────

# Root directory for persona definitions
# PERSONA_ROOT=/path/to/personas

# ─────────────────────────────────────────────────────────────────
# Logging & Memory
# ─────────────────────────────────────────────────────────────────

# Path to autosave echo log file
# ALTER_EGO_LOG_PATH=/path/to/chaos_echo_log.chaos

# Path to persona switch log file
# ALTER_EGO_SWITCH_LOG=/path/to/alter_switch_log.chaos

# Path to SQLite memory database
# MEMORY_DB=/path/to/alter_ego_memory.db

# ─────────────────────────────────────────────────────────────────
# Runtime Modes
# ─────────────────────────────────────────────────────────────────

# Use dummy LLM backend for testing (auto|on|off)
# ALTER_EGO_DUMMY_ONLY=auto

# Path to dummy LLM script (optional, for testing)
# ALTER_EGO_DUMMY_SCRIPT=/path/to/script.txt

# Disable CUDA for GPT4All (set to 1 to disable)
# GPT4ALL_NO_CUDA=1
